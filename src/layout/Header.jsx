import React from "react";
import { Box, Container } from "@mui/material";

const Header = () => {
  return <Container sx={{ backgroundColor: "orange" }}>Header</Container>;
};

export default Header;
